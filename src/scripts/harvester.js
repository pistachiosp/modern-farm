import { usePlants } from "./field"

//The harvestPlants function must accept the plants array as input
//the function will return an array of seed objects
//Add that many of the plant objects to the array that the function returns. For example, if the current plant is a peanut object with an output of 2...Then the array that the function returns should have two identical objects added to it.
export const harvestPlants = (fieldGrownPlants) => {


    
}